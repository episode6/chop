apply plugin: GroovyLib

dependencies {
  compile project(':chop-core')
  compile('com.google.code.findbugs:jsr305')
  testCompile("org.spockframework:spock-core")  {
    exclude module: 'groovy-all'
  }
}
