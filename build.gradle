// dokka's dependencies fail to resolve without this
buildscript { repositories { jcenter() } }

wrapper {
  gradleVersion = "4.9"
  distributionType = "all"
}

allprojects {
  repositories {
    jcenter()
    google()
  }

  group = "com.episode6.hackit.chop"
  version = "0.1.10"
}
